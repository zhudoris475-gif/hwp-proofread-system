# 작업 계획

## 목표
- `hwp_ollama_proofread_detailed.py`를 `--stage=all`로 실제 실행한다.
- 실행 전 `.bak` 복원과 텍스트 추출 자수 확인 절차를 유지한다.
- 현재 작업 폴더 기준 시스템 구조를 정리한다.
- COM 자동화 오류 원인, 해결 방침, 업데이트 구조를 문서 형태로 정리한다.

## 단계
- [complete] 1. 계획 파일 생성 및 현재 상태 기록
- [complete] 2. 실행 전 파일/규칙/로그 구조 점검
- [complete] 3. `.bak` 복원 후 `--stage=all` 실행
- [complete] 4. 로그와 결과 파일 수집
- [complete] 5. 시스템 구조 정리
- [complete] 6. 오류 원인 및 해결 방침 정리
- [complete] 7. 업데이트 구조 정리 및 최종 보고

## 이번 실행 결과
- 최근 실제 실행 시각은 `2026-04-16 23:52:50 ~ 23:52:55`이며, 기준 문서는 `system_review_20260416.md`이다.
- 64비트 Python에서는 `HWPFrame.HwpObject` 생성이 실패하고, 32비트 Python에서는 성공한다.
- 스크립트에는 64비트 감지 시 32비트 Python으로 재실행하는 부트스트랩이 이미 반영되어 있다.
- 현재 작업 폴더에 규칙 파일 3종을 배치했다.
- `.bak` 복원 후 `--stage=all` 실행 결과 총 교정 항목 `10개`, 총 적용 건수 `12건`이 확인되었다.
- 결과 리포트와 구조 문서를 생성했다.
- 기본 대상 폴더는 현재 환경 기준 자동 해석 구조로 수정했다.

## 시스템 구조
- 실행 스크립트: `hwp_ollama_proofread_detailed.py`
- 규칙 파일: `rules_documentation.txt`, `rules_china_place.txt`
- 로그 파일: `hwp_ollama_proofread_log.txt`
- 결과 리포트: `reports\*.txt`
- 작업 기록 문서: `task_plan.md`, `findings.md`, `progress.md`
- 통합 점검 문서: `system_review_20260416.md`
- 대상 원본/복원 파일: `*.hwp`, `*.hwp.bak`

## 오류 해결 방침
- 실행 전마다 반드시 `.bak` 복원 여부와 텍스트 추출 자수를 먼저 확인한다.
- 결과 확인 시 `항목수`, `매치 건수`, `실제 적용 건수`를 구분해서 본다.
- COM 오류는 64비트 경로보다 32비트 Python 자동 재실행 경로를 우선 기준으로 점검한다.
- 규칙 파일 누락 여부와 현재 작업 폴더 기준 경로 해석이 정상인지 먼저 확인한다.
- 세부 구조와 실행 이력은 `system_review_20260416.md`를 기준 문서로 참조한다.

## 오류 원인
- `서버 실행이 실패했습니다.`: 64비트 LocalServer 경로에서 `HwpApi.exe` DCOM 등록 타임아웃 발생
- `OLE error 0x800700c1`: 64비트 Python이 32비트 `HwpAutomation.dll`을 직접 로드하려다 실패
- `FileNotFoundError`: 초기에는 규칙 파일과 기본 HWP 경로 부재

## 권장 운영 순서
- `.bak` 파일로 원본을 먼저 복원한다.
- 복원 직후 텍스트 추출 자수를 확인한다.
- 규칙 파일 최신본과 현재 작업 폴더 배치 상태를 확인한다.
- `py -3` 실행 후 32비트 Python 자동 재실행 여부를 확인한다.
- 실행 후 `항목수`, `매치 건수`, `실제 적용 건수`, 리포트, 로그를 함께 검토한다.

## 2026-04-17 후속 계획

### 목표
- 3단계 가운데점에서 병렬 구조가 누락되지 않도록 교정 범위를 다시 맞춘다.
- 규칙 기반으로 교정되지 않는 가운데점 후보는 반드시 `Ollama` 검토 대상으로 남긴다.
- 수정되지 않은 가운데점/따옴표 후보도 로그에 `유지` 또는 `미교정`으로 남긴다.
- 단일 대상 파일 기준으로 `.bak` 복원 후 재실행하여 결과를 다시 검증한다.

### 현재 문제
- 최근 최적화 과정에서 `한자 포함 문맥`을 과하게 규칙 유지로 처리해 병렬 구조 일부가 누락되었다.
- 그 결과 일부 실행에서는 3단계 가운데점 `Ollama 대상 0개`로 집계되어 연동이 사실상 빠졌다.
- 수정되지 않은 후보가 모두 로그에 남지 않아 검토 추적성이 떨어졌다.
- 실행 도중 스크립트 동작 흐름이 달라져 단일 파일 기준 검증을 중단했다.

### 실행 단계
- [pending] 8. 현재 `hwp_ollama_proofread_detailed.py` 기준으로 3단계 가운데점 분류 로직 재점검
- [pending] 9. 병렬 구조 우선 교정 범위와 `Ollama` 후보 제외 조건 재조정
- [pending] 10. 수정 여부와 무관하게 후보별 로그가 남도록 `유지/미교정` 로그 보강
- [pending] 11. `.bak` 복원 후 단일 `작업본` 파일에 대해 `--stage=all` 재실행
- [pending] 12. 단계별 `항목수 / 매치 건수 / 실제 적용 건수 / 유지 건수` 재검증

### 검증 기준
- 순수 한글 병렬 구조는 기존 규칙대로 우선 교정되어야 한다.
- 규칙으로 확정되지 않는 가운데점 후보는 `Ollama 대상` 수치에 남아야 한다.
- 수정되지 않은 항목도 `가운데점 유지`, `가운데점 유지-문맥`, `따옴표 유지`, `응답없음` 등으로 로그에 확인되어야 한다.
- 재실행 결과는 최신 리포트와 `hwp_ollama_proofread_log.txt` 기준으로 확인한다.

### 주의 사항
- 실행 전 반드시 `.bak` 복원 후 시작한다.
- 단일 파일 검증 중에는 다중 파일 일괄 처리로 넘어가지 않는다.
- 사용자 수정이 개입된 상태일 수 있으므로 현재 파일 내용을 기준으로만 후속 수정한다.
